import React from "react";
import ReactDOM from "react-dom";


describe("Search", () => {    
    it("should return all products given searchString is empty", () => {
    });
    
    it("should return filtered products given searchString is not empty and product name exits", () => {
    });
    
    it("should return no products given searchString is not empty and product name does not exist", () => {
    });
});
